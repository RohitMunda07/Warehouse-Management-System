export default function Placeholder({ title }) {
  return (
    <div className="page-head">
      <div>
        <h2>{title}</h2>
        <div className="sub">This module isn't built yet — a natural next addition to your Express API and MongoDB schema.</div>
      </div>
    </div>
  );
}
